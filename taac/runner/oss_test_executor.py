# pyre-unsafe

"""
OSS Test Executor

Placeholder. Actual execution wiring (TaacRunner instantiation +
per-(playbook, dut) invocation + result capture) lands in a follow-up
PR. Exception → status mapping is centralized in classify_exception()
(oss_exception_classifier.py); this file is just the scaffold the
follow-up populates.
"""


class OSSTestExecutor:
    """Placeholder — the active executor is added when execution wires up."""

    pass
